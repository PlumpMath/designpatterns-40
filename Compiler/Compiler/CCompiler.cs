using DP_Tokenizer;

namespace Compiler
{
    class CCompiler
    {
        private TokenList<Token> _tokenList;

        public CCompiler(TokenList<Token> tokenList)
        {
            _tokenList = tokenList;
        }

        public void Compile()
        {
            
        }
    }
}
